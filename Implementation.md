# Implementation Plan

This document outlines the gaps identified based on [Specifications.md](file:///Users/ganaraja/repos/rag_pipeline/Specifications.md) and the proposed fixes.

## Proposed Changes

### Frontend
#### [MODIFY] [api.config.ts](file:///Users/ganaraja/repos/rag_pipeline/frontend/src/config/api.config.ts) (file:///Users/ganaraja/repos/rag_pipeline/frontend/src/config/api.config.ts)
- **Current Issue**: The `baseURL` is hardcoded to `'http://localhost:8000'`, which ignores the Vite proxy (`/api`) config and can cause cross-origin issues or connection failures depending on the environment.
- **Fix**: Update `baseURL` to use the Vite proxy (e.g., `baseURL: import.meta.env.VITE_API_URL || '/api'`), ensuring that API calls are properly routed to the backend.

### Backend
#### [MODIFY] [chunking_strategy.py](file:///Users/ganaraja/repos/rag_pipeline/backend/src/chunking_strategy.py) (file:///Users/ganaraja/repos/rag_pipeline/backend/src/chunking_strategy.py)
- **Current Issue**: Contains dummy placeholder functions returning hardcoded text/random data instead of the actual [docling](file:///Users/ganaraja/repos/rag_pipeline/backend/src/chunking_strategy.py#125-164), `chonkie`, and `jina` logic.
- **Fix**: Uncomment the dependencies and implement the actual logic:
  - Phase 1: Use `docling.document_converter.DocumentConverter` to extract parent chunks.
  - Phase 2: Use `chonkie.SemanticChunker` to semantically chunk each section.
  - Phase 3: Use LLM for contextual chunking (optional/enabled via config).
  - Phase 4: Use `jinaai/jina-embeddings-v2-base-en` for late chunking.

#### [MODIFY] [embedding_manager.py](file:///Users/ganaraja/repos/rag_pipeline/backend/src/embedding_manager.py) (file:///Users/ganaraja/repos/rag_pipeline/backend/src/embedding_manager.py)
- **Current Issue**: Uses `numpy.random` to generation dummy embeddings.
- **Fix**: Implement the real models from the notebook:
  - Matryoshka 768 & 64: `SentenceTransformer("tomaarsen/mpnet-base-nli-matryoshka")`
  - ColBERT: `fastembed.LateInteractionTextEmbedding("colbert-ir/colbertv2.0")`
  - SPLADE: `fastembed.SparseTextEmbedding("prithivida/Splade_PP_en_v1")`
  - Cross-Encoder: `SentenceTransformer.CrossEncoder("cross-encoder/ms-marco-MiniLM-L6-v2")`

#### [MODIFY] [retrieval_pipeline.py](file:///Users/ganaraja/repos/rag_pipeline/backend/src/retrieval_pipeline.py) (file:///Users/ganaraja/repos/rag_pipeline/backend/src/retrieval_pipeline.py)
- **Current Issue**: Uses a single class toggled by a boolean `use_prefetch`. 
- **Fix**: Split into exactly two classes as per the spec:
  1. `MultiEmbeddingRetrievalPipelineQdrant` (using [query_with_prefetch](file:///Users/ganaraja/repos/rag_pipeline/backend/src/qdrant_manager.py#199-278))
  2. [MultiEmbeddingRetrievalPipeline](file:///Users/ganaraja/repos/rag_pipeline/backend/src/retrieval_pipeline.py#30-491) (naive sequential filtering)

## Verification Plan

### Automated Tests
1. **Frontend**:
   - Run `npm run test` in `/Users/ganaraja/repos/rag_pipeline/frontend`.
   - Ensure the React components (especially `CollectionCreator`, `CollectionDeleter`, `FileUploader`, and `ChatInterface`) test suites pass.
2. **Backend**:
   - Run `PYTHONPATH=src pytest tests/` in `/Users/ganaraja/repos/rag_pipeline/backend`.
   - Verify that all unit and integration tests for the chunking strategy, embedding manager, and retrieval pipeline pass. Write new tests for the split pipeline classes if necessary.

### Manual Verification
1. Open the UI, navigate to Admin Tab, create a collection.
2. Upload a sample document and verify that the backend successfully chunks the document and stores embeddings in Qdrant.
3. Switch to User Tab and ask a query. Ensure that the multi-embedding pipeline works correctly and returns an answer generated by Ollama.
